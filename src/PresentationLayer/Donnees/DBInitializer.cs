using InfrastructureLayer.Data;

namespace PresentationLayer.Donnees
{
    public static class DBInitializer
    {
        public static void Initialize(ContratAutoDbContext contratAutoDbContext)
        {
            //if (contratAutoDbContext.ContratAutos.Any() && contratAutoDbContext.Voitures.Any() && contratAutoDbContext.Souscripteurs.Any() && contratAutoDbContext.Personnes.Any())
            //{
            //    return;
            //}
            //var contratAutos = new ContratAuto();
            //contratAutoDbContext.ContratAutos.AddRangeAsync();
        }
    }
}
